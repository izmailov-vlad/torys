import 'package:flutter/material.dart';
import 'package:torys/core/presentation/entities/book.dart';

class BestBookByAuthor extends StatelessWidget {
  final Book book;
  const BestBookByAuthor({Key? key, required this.book}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
