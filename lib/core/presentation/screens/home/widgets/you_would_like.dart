import '../../../../../ui.dart';

class YouWouldLike extends StatelessWidget {
  const YouWouldLike({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
